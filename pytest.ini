[pytest]
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning

# Collect tests only from backend directory and project root to exclude documentation scripts and heavy benchmarks
testpaths =
    backend
    .

# Ignore documentation and script directories that are not part of unit test suite
addopts = --ignore=docs